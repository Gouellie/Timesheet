<Window x:Class="Timesheet.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Timesheet"
        xmlns:v="clr-namespace:Timesheet.Views"
        xmlns:vm="clr-namespace:Timesheet.ViewModels" 
        mc:Ignorable="d"
        Title="{Binding VersionString, StringFormat=CaaM Timesheet : v{0}}"
        MinHeight="400" MinWidth="300"
        Height="400" Width="750">
    <Window.DataContext>
        <vm:MainWindowViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="3*"/>
        </Grid.RowDefinitions>

        <v:TimeCardView Grid.Row="0"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <v:UsersView Grid.Column="0"/>
            <v:TimeStampsView Grid.Column="1"/>

        </Grid>

    </Grid>
</Window>
